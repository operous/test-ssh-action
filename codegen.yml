overwrite: true
schema: "https://app.operous.dev/graphql"
generates:
  ./src/generated/requests.ts:
    documents: ./src/queries/*.graphql
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
